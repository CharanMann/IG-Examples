{
  "globalDecorators": {
    "capture": "all"
  },
  "baseURI": "https://openam-tenantName-dev.id.forgerock.io",
  "condition": "${matches(request.uri.path,'^/am')}",
  "handler": {
    "type": "Chain",
    "config": {
      "filters": [
        {
          "name": "hostHeader",
          "type": "HeaderFilter",
          "config": {
            "messageType": "REQUEST",
            "remove": [
              "host"
            ],
            "add": {
              "host": [
                "openam-tenantName-dev.id.forgerock.io"
              ]
            }
          }
        }
      ],
      "handler": "FIDCClientHandler"
    }
  }
}
